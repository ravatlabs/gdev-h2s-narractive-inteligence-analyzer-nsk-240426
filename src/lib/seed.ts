/**
 * Database seeding is now handled in-memory within lib/db.ts 
 * to ensure compatibility with restricted environments.
 */
export function seedDatabase() {
  console.log("Mock database is active.");
}
